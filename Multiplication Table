#include <stdio.h>

int main()

{
    
    int n;
    
	printf("Enter a whole number:\n");
	scanf("%d", &n);
	printf("The %d Times Table:\n------------------\n\n", n);
	
	for (int i=0; i<15; i++)
	{
	    int p = i*n;
	    printf("%d x %d = %d\n", i, n, p);
	}
	
	return 0;

}
